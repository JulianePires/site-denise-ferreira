import { Logo } from '@components/Logo';
import { ContainerHeader } from './Header.styled';

export function Header() {
  return (
    <ContainerHeader>
      <Logo />
    </ContainerHeader>
  );
}
